spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/claim_service
    username: postgres
    password: postgres

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
